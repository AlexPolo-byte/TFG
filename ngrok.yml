version: "2"
tunnels:
  backend:
    addr: backend_telegram:5000
    proto: http
    domain: chronometrically-unbreaking-kyrie.ngrok-free.dev
  grafana:
    addr: grafana:3000
    proto: http
  rabbitmq:
    addr: rabbitmq:15672
    proto: http
